/p Joe created this partial
p
  - if local_assigns[:user_counter]
    => "#{local_assigns[:user_counter]})"
  | Captain
  span#user-name
    strong =<> user.name
  | is a
  span#user-sexuality
    strong =< user.sexuality
